# Database configuration for University Management System
# The project uses the primary Aiven Cloud database below by default.

# Primary Connection (Aiven Cloud)
db.url=jdbc:mysql://:18087/universitymanagementsystem?useSSL=true&requireSSL=true&verifyServerCertificate=false&serverTimezone=UTC
db.user=avnadmin
=

# Backup Localhost Connection (Uncomment these if the cloud database is offline)
# db.url=jdbc:mysql://localhost:3306/universitymanagementsystem?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
# db.user=root
# =
