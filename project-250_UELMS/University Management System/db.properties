# Database configuration for University Management System
# This repository intentionally does not store real database credentials.
# Copy this file locally to db.properties and provide your own values,
# or set DB_URL, DB_USER, and DB_PASS in your environment.

db.url=
db.user=
=
